# Everyone (and CI) builds with the same stable toolchain + the components the
# gate needs, so "works on my machine" formatting/lint drift can't happen.
[toolchain]
channel = "1.94.1"
components = ["rustfmt", "clippy"]
